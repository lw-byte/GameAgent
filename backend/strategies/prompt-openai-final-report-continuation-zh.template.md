<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
<!-- Copyright (C) 2024-2026 Gracker (Chris) | SmartPerfetto -->

系统校验：所有分析阶段已经 completed/skipped，但当前还没有面向用户的完整最终报告。

请现在只输出最终报告正文；第一行必须是 `## 综合结论`。不要再调用工具，不要再调用 update_plan_phase，不要解释过程，不要输出“验证逻辑/现在进入 Phase/完成综合结论输出/分阶段证据摘要”。

报告必须包含：综合结论、关键证据链、根因拆解、已排除因素、优化建议、置信度/限制。

必须继续遵守本轮场景策略、Final Report Contract 和最后一个 next_phase_reminder 的输出约束。若场景策略或契约要求根因分布、代表样本、阶段耗时拆分、双受众建议、架构分支判断或其他专属结构，最终报告必须保留这些结构，不能降级为短摘要。
如果 Final Report Contract 给出了必检项名称，优先用同名小节或清晰标签呈现，例如“阶段耗时分解”、“[App层]”、“[系统/平台层]”，避免只把契约内容隐含在叙述里。
如果缺失项是“App/系统分层建议”，必须补出同名小节，并在小节内同时包含“[App 层]”和“[系统/平台层]”；没有可操作项的一侧也要明确写成限制或数据不足，不能省略。
先输出 Final Report Contract 要求的必需结构，再输出长树状图、附录或扩展说明；不要把必需的优化建议、分层建议、限制说明放在长代码块或长树之后，以免报告中途截断时缺失关键结论。

每个关键结论必须保留证据类型与边界：说明它来自直接 trace、Skill/SQL 派生指标、日志/快照、外部聚合、诊断 API、用户上下文还是缺失证据。涉及 Android/API/设备能力、Play Vitals、App Performance Score、A/B 或线上 APM 时，必须写明这是版本/政策敏感或外部聚合信号，不能把它当成当前 trace 的直接根因。缺失数据只能写成限制和下一步采集动作，不能写成“没有该问题”。

每个 `[CRITICAL]`（包括优化建议）都必须在同一发现内保留已观测的量化证据或可定位 source/artifact 引用。不能只写预期收益，不能只把它降为 `[HIGH]` 来规避证据要求；若现有证据不足，请去掉 CRITICAL 标记并明确写成待验证建议或未知项。

同时执行内核等待语义自检：`epoll` / `poll` 类 `blocked_function` 通常表示等待事件或空闲，不能写成磁盘或文件 IO 根因；只有 `io_wait=1`、IO/page-cache 函数族，或应用层文件/数据库证据能够补强时，才可把它列为 IO 候选。`blocked_function` 是 kernel wchan 单帧，不是完整内核调用栈。

`D/DK/Q4a` 单独只能命名为“不可中断等待”，不能在标题、表格分类或已排除因素中改称“I/O 阻塞”“磁盘 I/O”或“轻度 I/O”。低占比、轻度只能描述 D-state 的数量级。只有 `io_wait=1`、IO/page-cache `blocked_function` 或应用层文件/数据库证据与同一线程和等待窗口明确绑定时，才可按证据强度写成 I/O 候选或贡献因素；只有完整因果证据链才可写成根因。缺少这些证据时必须写“无法归因 I/O”，不能反向写成“没有 I/O 问题”。

不要只复述阶段摘要；必须把已采集到的具体数值和证据串成可读结论。

完整性优先。可以用小型聚合表压缩信息；不要展开逐阶段流水账，不要复制 artifact 原表，不要重复 SQL 原文，不要输出“数据来源索引/证据表索引”（系统会自动生成）。若证据很多，优先保留关键证据链、场景契约要求的结构和最高优先级根因，不要为了压缩长度裁剪关键结论或证据。
